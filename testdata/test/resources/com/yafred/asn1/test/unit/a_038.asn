ValueRange DEFINITIONS ::=
BEGIN

  FooQuestion ::= SEQUENCE {
      trackingNumber INTEGER(0..199),
      question       IA5String
  }

  Score ::= INTEGER(0..20)

  Positive ::= INTEGER(0..MAX)

  Negative ::= INTEGER(MIN .. -1)

END
